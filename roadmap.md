# Roadmap

## In progress
- [x] Auto-assign unique colours per subject even without a course catalogue (new batches / ICS without codes)
- [x] Show `S{lecture number}` from the ICS instead of the section (`S1`)
- [x] Remove the live tracker sub-view from Attendance; drop "absentee workbook" wording
- [ ] Mark-absent buttons for the day's classes on the Feed page

## New
- [ ] Activity/notification section (member joined, who, which batch, what changed)
- [ ] Remove the member-approval UI (members are auto-approved on signup)
- [ ] Fix members list so all batch members are visible (RLS / query check)
